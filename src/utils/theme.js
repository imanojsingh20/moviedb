import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#1C1F22",
    },
    secondary: {
      main: "#FC6948",
    },
    background: {
      default: "#181D23",
    },
    text: {
      primary: "#fafafa",
    },
  },
});

export default theme;
